# analyze-docssサブエージェントで空応答（tool_calls無し・本文無し）による自動リトライが短時間に2回発生

- **区分**: 問題点
- **検知日時**: 2026-08-12 11:57:51〜11:59:12
- **対象ログファイル**: data/logs/app_20260812_113919.log

## 経緯

[issue/20260812_115000_explore_docs_batch_oversize_hard_token_cutoff.md](20260812_115000_explore_docs_batch_oversize_hard_token_cutoff.md)・[issue/20260809_002501_glob_wrong_path_inference_error.md](20260809_002501_glob_wrong_path_inference_error.md)の追記と同じ一連のタスク（子供会活動記録からの年間行事予定表作成）で、残り2年分（2024・2025）を`analyze-docss`へ再委譲した際、Globパス誤りからの自己回復（11:57:09）後、`[subagent].empty_response_max_retries`(2)による「空の応答検知→再試行」が2回発生した。いずれも`(1/2回目)`表記で、カウンタが使い切られる前に自動リトライで継続できている（この時点ではまだタスク自体は完了しておらず、最終的に打ち切りに至ったかは未確認）。

## ログ引用

```
2026-08-12 11:57:51,953 WARNING src.subagent: subagent: 空の応答（tool_calls無し・本文無し）を検知したため再試行します(1/2回目)
2026-08-12 11:59:12,211 WARNING src.subagent: subagent: 空の応答（tool_calls無し・本文無し）を検知したため再試行します(1/2回目)
```

## 推定原因

未検証。2回とも`(1/2回目)`と表示されている点が気になる（同一サブエージェント実行内で連続発生したなら2回目は`(2/2回目)`になるはずだが、両方`1回目`表記＝カウンタが途中でリセットされている、または実際には別々の反復タイミングで独立に1回ずつ発生している可能性がある）。直前のGlobパス誤り訂正（11:56:48〜11:57:09）で消費したツール呼び出し・情報量が多く、モデルが次の一手を決めきれず空応答を返した可能性があるが、`src/subagent.py`の該当カウンタのスコープ（イテレーション単位かサブエージェント生存期間単位か）を未確認のため断定できない。

## 追記（YYYY-MM-DD HH:MM）

（同一原因の問題が再検知されるたびに、ここに追記を積み重ねていく）

## 追記（2026-08-12 12:15）

同一タスク・同一worker実行の続きで3回目の発生（今回も`(1/2回目)`表記）。

```
2026-08-12 12:13:15,739 WARNING src.subagent: subagent: 空の応答（tool_calls無し・本文無し）を検知したため再試行します(1/2回目)
```

直前（12:13:07）は`execute_python_code`が成功しており（`AttributeError: 'MergedCell'`クラッシュ・[issue/20260812_121000_excel_edit_mergedcell_attributeerror.md](20260812_121000_excel_edit_mergedcell_attributeerror.md)からの立て直しで、`ops_v3.json`を新規生成する大きめのコード実行）、その直後の1手で空応答になっている。3回とも共通して「直前に大きめのツール呼び出し・情報量の多い処理があった直後」というパターンが見える。

また3回連続で`(1/2回目)`表記だったことから、カウンタは「イテレーション単位でリセットされる、直近の空応答1回に対して2回まで再試行できる」設計である可能性が高まった（毎回1回のリトライで回復できているため、2回目に到達する前に正常化している）。バグではなく想定内の動作である可能性が高いが、`src/subagent.py`のソースは今回も未確認のため引き続き未確定。

## 追記（2026-08-22 21:12）

別タスク（家計簿xlsm作成、excel-vba-edit/excel-edit）でも4回目の発生。
今回も`(1/2回目)`表記で、直前に大きめの`execute_python_code`（長期
キャッシュフロー用の行データ組み立て、21:12:06）があった直後の1手で
空応答になっている点も含め、これまでのパターンと一致する。

```
2026-08-22 21:12:31,156 WARNING src.subagent: subagent: 空の応答（tool_calls無し・本文無し）を検知したため再試行します(1/2回目)
```

直後（21:13:53）には`read_skill`呼び出しが正常に再開しており、今回も
1回のリトライで自己回復している。「直前に情報量の多いツール呼び出しが
あった直後に空応答→1回のリトライで正常化」というパターンが4回連続で
再現しており、想定内の動作（設計通りのリトライ機構）という見立てが
さらに補強された。`src/subagent.py`のリトライカウンタ実装は依然未確認
だが、実害（タスク継続不能）は一度も出ていないため、優先度は低いまま
据え置く。

## 追記（2026-08-23 00:37）

5回目の発生。今回も同じ家計簿xlsm作成タスク（再起動後）で、直前に
CSVファイル（銀行入出金明細2件）の全行確認という情報量の多い
`execute_python_code`（00:36:54）があった直後の1手で空応答になっている。

```
2026-08-23 00:37:17,557 WARNING src.subagent: subagent: 空の応答（tool_calls無し・本文無し）を検知したため再試行します(1/2回目)
```

直後（00:38:34）にはVBAコード生成の`execute_python_code`が正常に再開して
おり、今回も1回のリトライで自己回復している。5回連続で同じパターン
（直前に情報量の多いツール呼び出し→空応答→1回のリトライで正常化、実害
なし）が再現しており、想定内の動作という見立てを維持する。

## 追記（2026-08-23 20:16）

6回目の発生。今回は魚図鑑PPTX生成タスクで、`analyze_image`により10枚の
画像を分析した直後の1手で発生（対象ログファイル
data/logs/app_20260823_195217.log）。

```
2026-08-23 20:15:33,201 WARNING src.subagent: subagent: 空の応答（tool_calls無し・本文無し）を検知したため再試行します(1/2回目)
```

直前のLLM応答は`content=''`だが`reasoning_content`には10枚分の分析結果
（表形式）が正常に含まれており、末尾が`</parameter>`という不自然な
XMLタグ様の文字列で終わって`tool_calls=[]`になっていた。情報量の多い
出力（画像分析結果の表）を書き終えた直後にツール呼び出し/終了応答の
どちらの形式でも出力しきれず空応答になる、という従来からのパターンと
一致。今回も1回のリトライで正常化しており実害なし。

## 追記（2026-08-23 20:34）

7回目の発生。同一の魚図鑑PPTX作成タスクの続き（対象ログファイル
data/logs/app_20260823_195217.log）。

```
2026-08-23 20:32:07,942 WARNING src.subagent: subagent: 空の応答（tool_calls無し・本文無し）を検知したため再試行します(1/2回目)
```

## ユーザー回答

ここにはユーザーの回答が記述される
